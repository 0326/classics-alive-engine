# 古籍活化游戏引擎：Agent 协作约束

## 项目定位

构建“古籍活化游戏引擎”：从古籍、注释和已授权的辅助资料生成可审阅、可追溯的 Web 游戏。首个交付形态是基于 Ink 的视觉小说；引擎不得因此限制未来的互动地图、经营、解谜或模拟玩法。

## 不可违背的原则

1. 将原始古籍、释文、史实断言、叙事改编和生成资产分层保存，不得混写。
2. 不得将推断、现代解释或虚构台词伪装成原文或确定史实。
3. 每个会影响“正史路线”的选择，必须能追溯到一个或多个已审核的 `claim`。
4. 不得直接手改生成目录；修改内容源后运行内容构建与验证。
5. 不得为了通过测试删除史料、分支或失败结局；应修复引用、结构或规则本身。
6. 先交付一个可验证的垂直切片，再扩展到整部典籍或可视化编辑器。

## 内容状态

所有内容必须声明下列状态之一：

- `explicit`：原典明确记载。
- `inferred`：由原典上下文或已批准的研究推断。
- `contested`：存在可靠但冲突的解释或版本。
- `invented`：为游戏体验新增的内容。
- `counterfactual`：明确标示的假设历史分支。

## 目录边界

- `content/`：可由人工和 Agent 编辑的内容源。
- `packages/`：通用、无业务典籍依赖的代码。
- `apps/`：玩家或编辑者应用，不得被 `packages/` 反向依赖。
- `generated/`：仅由构建脚本写入。
- `skills/`：跨 Agent 的规范化工作流；脚本必须可脱离聊天会话执行。

## Agent 工作方式

1. 修改前先阅读相关 schema、内容包和已有验证器。
2. 对历史内容变更，先产出或更新 `claim`，再修改故事或学习材料。
3. 对运行时代码变更，至少运行类型检查、单元测试和相关内容验证。
4. 对玩家路径变更，运行正史路径校验与浏览器试玩。
5. 报告中明确列出：修改内容、验证结果、尚需人工史学审核的事项。

## 人工审核节点

- 输入版本、版权及来源。
- `contested` 与 `inferred` claim 的采纳。
- 人物价值判断、死亡解释和假设历史结局。
- 对外发布前的文化、叙事、美术与无障碍审阅。
